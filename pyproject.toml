[project]
name = "csv-duck-dbt-fun"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dbt-core>=1.11.9",
    "dbt-duckdb>=1.10.1",
    "duckdb>=1.5.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
